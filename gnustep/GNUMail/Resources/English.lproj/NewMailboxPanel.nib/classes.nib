{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancelClicked = id; okClicked = id; }; 
            CLASS = NewMailboxPanelController; 
            LANGUAGE = ObjC; 
            OUTLETS = {mailboxNameField = NSTextField; popUpButton = NSPopUpButton; }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}